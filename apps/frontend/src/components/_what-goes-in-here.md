# What goes in components/?

- Basic, generic and reusable UI pieces
- These should be _"dumb"_, meaning that they most not do any intrinsic logic that is not passed with props
- These components should never "know" where they are being used (generic!)
- "Keep these clean and self-contained. If they start doing data fetching, kick them out to `features/`"
