# What goes in pages/?

- Route-level components
- These are the ones the router points to
- They are the wrapper of the `features` and `components`, orchestrating them into something meaningful
